assembly-versioning-scheme: MajorMinorPatchTag
mode: ContinuousDeployment
branches:
  master:
    tag: beta
    regex: (origin/)?master
  pull-request:
    mode: ContinuousDeployment
    tag: PR
ignore:
  sha: []
next-version: 2.0.1